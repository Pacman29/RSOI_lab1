dist: trusty
sudo: false
language: Java
jdk: oraclejdk8

stages:
  - name: test
  - name: deploy


jobs:
  include:
    - stage: test
      install: mvn --version
      script:
        - mvn install
        - mvn test
    - stage: deploy
      install: mvn --version
      script:
        - mvn install
        - mvn package
        - docker build -t rsoitest .

