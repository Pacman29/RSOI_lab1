language: generic

stages:
  - name: test
  - name: deploy


jobs:
  include:
    - stage: test
      language: java
      script:
        - mvn install
        - mvn test
    - stage: deploy
      language: java
      script:
        - mvn install
        - mvn package
        - docker build -t rsoitest .

